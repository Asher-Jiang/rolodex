{
  "name": "personal-rolodex",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "install:mac": "npm run build && node scripts/macos/install.mjs",
    "icon": "node scripts/macos/render-icon.mjs",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "db:migrate": "npm run db:migrate -w backend",
    "import:notion": "npm run import:notion -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
